---
phase: 03-agent-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/analyst.md
  - .claude/agents/orchestrator.md
autonomous: true
requirements: [ANLST-01, ANLST-02, ANLST-03, ANLST-04, ANLST-05, ORCH-01, ORCH-02]

must_haves:
  truths:
    - "Analyst Step 5 worst case section specifies the 5-step structured process (identify trough inputs, run floor calc, TBV cross-check, analyst adjustment, show trough path)"
    - "Analyst instructions require running calc-score.sh floor BEFORE writing any worst-case narrative"
    - "Analyst output format includes trough path table in the worst case section"
    - "Orchestrator has a downside compliance audit step that checks for floor calc, heroic optimism flags, and TBV cross-check"
    - "Orchestrator rejects candidates missing any of the three downside compliance requirements"
  artifacts:
    - path: ".claude/agents/analyst.md"
      provides: "Structured worst-case process with trough anchoring"
      contains: "calc-score.sh floor"
    - path: ".claude/agents/orchestrator.md"
      provides: "Downside compliance audit step"
      contains: "Downside Compliance Audit"
  key_links:
    - from: ".claude/agents/analyst.md"
      to: "scripts/calc-score.sh"
      via: "floor command invocation in Step 5 worst case"
      pattern: "calc-score.sh floor"
    - from: ".claude/agents/analyst.md"
      to: "knowledge/strategy-rules.md"
      via: "reference to trough input anchoring table"
      pattern: "strategy-rules.md"
    - from: ".claude/agents/analyst.md"
      to: "knowledge/valuation-guidelines.md"
      via: "reference to Heroic Optimism test and TBV cross-check"
      pattern: "valuation-guidelines.md"
    - from: ".claude/agents/orchestrator.md"
      to: ".claude/agents/analyst.md"
      via: "audit checks analyst output for trough path, floor calc, heroic optimism"
      pattern: "trough path|floor calc|Heroic Optimism"
---

<objective>
Wire the analyst and orchestrator agents to use the trough-anchored worst case specification from knowledge files. Analyst gets a structured 5-step worst case process; orchestrator gets a downside compliance audit that rejects non-compliant candidates.

Purpose: The knowledge files (Phase 2) define WHAT the rules are. This plan makes the agents FOLLOW them. Without this wiring, the specs are just documentation.
Output: Updated analyst.md and orchestrator.md with structured worst-case process and compliance audit.
</objective>

<execution_context>
@/home/laudes/.claude/get-shit-done/workflows/execute-plan.md
@/home/laudes/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calculator/01-01-SUMMARY.md
@.planning/phases/02-knowledge-files/02-01-SUMMARY.md

@.claude/agents/analyst.md
@.claude/agents/orchestrator.md
@knowledge/strategy-rules.md
@knowledge/valuation-guidelines.md
@knowledge/scoring-formulas.md

<interfaces>
<!-- Key specs the executor needs from knowledge files (already written in Phase 2). -->

From knowledge/strategy-rules.md — 5-Step Structured Process:
- Step A: Identify trough inputs from 5yr FMP data already fetched in Step 3
- Step B: Run `calc-score.sh floor` with trough inputs to get mechanical floor price
- Step C: Cross-check floor against tangible book value per share (TBV cross-check from valuation-guidelines.md)
- Step D: Analyst adjustment — pessimism free, optimism triggers Heroic Optimism flag
- Step E: Show "trough path" table mapping each input to fiscal year and FMP data point

Trough Input Anchoring Table:
| Input | Trough Anchor | Source |
|-------|---------------|--------|
| Revenue | Lowest trailing-12-month revenue in 5yr FMP history | `income` endpoint |
| FCF Margin | Lowest annual FCF margin in 5yr FMP history | `cashflow` / `income` endpoints |
| FCF Multiple | Industry baseline from valuation-guidelines.md MINUS full discount schedule | `valuation-guidelines.md` |
| Shares | Current diluted shares (no buyback credit in worst case) | `metrics` endpoint |

Trough Path Format:
| Input | Trough Value | Fiscal Year | FMP Data Point |
|-------|-------------|-------------|----------------|
| Revenue | $2.2B | FY2022 | income statement, trailing 12mo |
| FCF Margin | 7.0% | FY2021 | cashflow / income |
| FCF Multiple | 10x | — | Industry 15x minus discounts (-3x leverage, -2x decline) |
| Shares | 130M | Current | metrics, diluted |

From knowledge/valuation-guidelines.md — Heroic Optimism triggers:
- Trough revenue above actual lowest TTM revenue in 5yr FMP history
- Trough FCF margin above actual lowest annual FCF margin in 5yr FMP history
- Trough FCF multiple above industry baseline (before discounts)
- Analyst adjusted floor UPWARD without documenting why trough conditions are implausible

From knowledge/valuation-guidelines.md — TBV Cross-Check:
- Floor > 2x TBV/share → flag for review
- TBV/share negative → note in trough path (solvency risk)
- Floor < TBV/share → no flag (pessimistic is OK)

From scripts/calc-score.sh — floor command signature:
```bash
bash scripts/calc-score.sh floor <revenue_b> <margin_pct> <multiple> <shares_m> <current_price>
```
Returns JSON: { revenue_b, fcf_margin_pct, fcf_b, multiple, shares_m, current_price, floor_price, downside_pct }

Current analyst.md locations to modify:
- Lines 196-199: "Reasonable Worst Case" section (REPLACE with structured 5-step process)
- Lines 238: Step 6 references "worst case in Step 5" (UPDATE reference)
- Lines 322: Output format worst case line (EXPAND to include trough path table)

Current orchestrator.md location to add:
- Between Step 4a (Epistemic Confidence Review, ends ~line 224) and Step 4b (Multiple Consistency Audit, starts ~line 228): INSERT new "4a2. Downside Compliance Audit" step
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace analyst.md worst case with structured 5-step process</name>
  <files>.claude/agents/analyst.md</files>
  <action>
Make three targeted edits to `.claude/agents/analyst.md`:

**Edit 1 — Replace "Reasonable Worst Case" section (lines 196-199)**

Replace the current 3-line "Reasonable Worst Case" block:
```
**Reasonable Worst Case:**
- What if revenue grows slower? Margins don't recover? Multiple stays low?
- Calculate floor price under pessimistic (but not catastrophic) scenario
- Express as % downside from current price
```

With the full structured worst case process:

```markdown
**Trough-Anchored Worst Case (Required):**

The worst case uses the same 4-input formula with trough inputs anchored to 5yr FMP historical data. Same stock + same data = same downside estimate. The mechanical floor is the starting point, not the final answer. See `$KNOWLEDGE_DIR/strategy-rules.md` Step 5 for the full specification.

**Step A — Identify trough inputs** from 5yr FMP data already fetched in Step 3:

| Input | Trough Anchor | Source |
|-------|---------------|--------|
| Revenue | Lowest trailing-12-month revenue in 5yr FMP history | `income` endpoint |
| FCF Margin | Lowest annual FCF margin in 5yr FMP history | `cashflow` / `income` endpoints |
| FCF Multiple | Industry baseline from valuation-guidelines.md MINUS full discount schedule | `valuation-guidelines.md` |
| Shares | Current diluted shares (no buyback credit in worst case) | `metrics` endpoint |

**Step B — Run the floor calculator** (MUST run before writing any worst-case narrative):
```bash
bash scripts/calc-score.sh floor <revenue_b> <margin_pct> <multiple> <shares_m> <current_price>
```
Show the command AND its JSON output in the analysis. The floor_price and downside_pct from this output are the mechanical starting point.

**Step C — TBV cross-check:**
Fetch tangible book value per share from the most recent quarterly balance sheet:
```bash
bash scripts/fmp-api.sh balance TICKER
```
Compute: TBV = Total Assets - Total Liabilities - Intangible Assets - Goodwill. Then TBV/share = TBV / diluted shares.

| Condition | Action |
|-----------|--------|
| Floor > 2x TBV/share | Flag: "TBV flag: floor ${floor} > 2x TBV/share ${tbv_share} — review for optimism" |
| TBV/share is negative | Note: "Negative TBV — structural solvency risk, floor should reflect this" |
| Floor < TBV/share | No flag needed (pessimistic is acceptable) |

**Step D — Analyst adjustment (asymmetric override):**
- Making the floor HARSHER (event risk, litigation, structural concerns): freely allowed, no flag needed
- Making the floor MORE OPTIMISTIC than mechanical calculation: triggers **Heroic Optimism** flag
- If Heroic Optimism triggered: provide 1-2 sentence justification explaining why trough conditions are implausible. If justification cannot be articulated clearly, use the mechanical floor without adjustment.
- See `$KNOWLEDGE_DIR/valuation-guidelines.md` "Worst-Case Heroic Optimism Test" for the four trigger conditions.

**Step E — Show trough path** (required in output):

| Input | Trough Value | Fiscal Year | FMP Data Point |
|-------|-------------|-------------|----------------|
| Revenue | ${value} | FY{year} | income statement, trailing 12mo |
| FCF Margin | {value}% | FY{year} | cashflow / income |
| FCF Multiple | {value}x | — | Industry {baseline}x minus discounts ({detail}) |
| Shares | {value}M | Current | metrics, diluted |

The final downside_pct from Step B (or Step D if adjusted) feeds into Step 6 scoring.
```

**Edit 2 — Update Step 6 downside reference (line 238)**

Change:
```
1. **Estimate downside %** (from worst case in Step 5)
```
To:
```
1. **Use downside %** from the trough-anchored worst case in Step 5 (floor_price output from calc-score.sh floor, or analyst-adjusted value if Step D applied)
```

**Edit 3 — Update output format worst case line (line 322)**

Replace:
```
**Worst Case:** ${floor} ({n}% downside) — {scenario description}
```
With:
```markdown
**Worst Case (Trough-Anchored):**
- Floor command: `calc-score.sh floor {rev_b} {margin} {multiple} {shares} {price}` → ${floor_price} ({n}% downside)
- TBV cross-check: TBV/share ${tbv} — {flag or "no flag"}
- Adjustment: {None / Harsher: {reason} / More optimistic: {justification} — HEROIC OPTIMISM FLAG}
- **Trough Path:**
  | Input | Trough Value | Fiscal Year | FMP Data Point |
  |-------|-------------|-------------|----------------|
  | Revenue | ${value} | FY{year} | {source} |
  | FCF Margin | {value}% | FY{year} | {source} |
  | FCF Multiple | {value}x | — | {discount path} |
  | Shares | {value}M | Current | {source} |
```

Do NOT modify any other sections of analyst.md. Preserve all existing content exactly.
  </action>
  <verify>
    <automated>grep -c "Trough-Anchored Worst Case" .claude/agents/analyst.md && grep -c "calc-score.sh floor" .claude/agents/analyst.md && grep -c "Step A" .claude/agents/analyst.md && grep -c "Step B" .claude/agents/analyst.md && grep -c "Step C" .claude/agents/analyst.md && grep -c "Step D" .claude/agents/analyst.md && grep -c "Step E" .claude/agents/analyst.md && grep -c "TBV cross-check" .claude/agents/analyst.md && grep -c "Heroic Optimism" .claude/agents/analyst.md && grep -c "Trough Path" .claude/agents/analyst.md && echo "---" && ! grep -q "Reasonable Worst Case" .claude/agents/analyst.md && echo "PASS: old section removed"</automated>
  </verify>
  <done>
    - Analyst Step 5 contains the 5-step structured worst case process (Steps A-E)
    - Step B requires running calc-score.sh floor BEFORE writing narrative
    - Step C specifies TBV cross-check with flag conditions
    - Step D specifies asymmetric override with Heroic Optimism flag
    - Step E requires trough path table in output
    - Output format includes trough path table, floor command, TBV check, and adjustment status
    - Old "Reasonable Worst Case" section fully replaced
    - Step 6 references trough-anchored downside from Step 5
  </done>
</task>

<task type="auto">
  <name>Task 2: Add downside compliance audit to orchestrator.md</name>
  <files>.claude/agents/orchestrator.md</files>
  <action>
Make two targeted edits to `.claude/agents/orchestrator.md`:

**Edit 1 — Insert "4a2. Downside Compliance Audit" step**

Insert a new subsection AFTER step 4a (Epistemic Confidence Review) and BEFORE step 4b (Multiple Consistency Audit). Place it after the line containing "8. **Apply binary outcome override**" (approximately line 224) and before "### 4b. Multiple Consistency Audit" (approximately line 228).

New section content:

```markdown
### 4a2. Downside Compliance Audit

After epistemic review, before the multiple consistency audit, verify each scored candidate's worst-case methodology:

1. **Floor calc check**: Confirm the analyst output contains a `calc-score.sh floor` command invocation with JSON output. The trough path must trace each of the 4 inputs (revenue, FCF margin, multiple, shares) to a specific FMP data point.
   - **Missing floor calc** → reject: move to "Rejected at Analysis" with reason: "downside non-compliant: no floor calc"

2. **Heroic Optimism check**: Scan for any unresolved Heroic Optimism flags. A flag is "unresolved" if the analyst adjusted the floor upward without providing a 1-2 sentence justification, OR if justification was provided but any of these conditions are true:
   - Trough revenue used is above the company's actual lowest TTM revenue in 5yr FMP history
   - Trough FCF margin used is above the company's actual lowest annual FCF margin in 5yr FMP history
   - Trough FCF multiple used is above the industry baseline (before discount schedule)
   - **Unresolved heroic optimism** → reject: move to "Rejected at Analysis" with reason: "downside non-compliant: unresolved Heroic Optimism flag — {specific trigger}"

3. **TBV cross-check**: Confirm the analyst output includes a TBV/share comparison. If Floor > 2x TBV/share, confirm the analyst noted the flag.
   - **Missing TBV cross-check** → reject: move to "Rejected at Analysis" with reason: "downside non-compliant: TBV cross-check skipped"
   - **TBV flag present but unaddressed** → add `downside_tbv_warning` confidence flag (does NOT reject — TBV is a sanity check, not a binding constraint)

4. **Trough path completeness**: Verify the trough path table has all 4 rows (Revenue, FCF Margin, FCF Multiple, Shares) with non-empty Fiscal Year and FMP Data Point columns.
   - **Incomplete trough path** → reject: move to "Rejected at Analysis" with reason: "downside non-compliant: incomplete trough path — missing {input}"

Candidates passing all 4 checks proceed to the multiple consistency audit. Add a line in the methodology notes section of the report:
```
- Downside methodology audited: floor calc present, Heroic Optimism resolved, TBV cross-check complete, trough path verified
```
```

**Edit 2 — Update the report template methodology notes**

In the report format section (around line 350), after the existing methodology note about calc-score.sh, add:
```
- Downside compliance audited: trough-anchored floor calc, Heroic Optimism resolution, TBV cross-check, trough path completeness
```

Do NOT modify any other sections of orchestrator.md. Preserve all existing content exactly.
  </action>
  <verify>
    <automated>grep -c "Downside Compliance Audit" .claude/agents/orchestrator.md && grep -c "Floor calc check" .claude/agents/orchestrator.md && grep -c "Heroic Optimism check" .claude/agents/orchestrator.md && grep -c "TBV cross-check" .claude/agents/orchestrator.md && grep -c "Trough path completeness" .claude/agents/orchestrator.md && grep -c "downside non-compliant" .claude/agents/orchestrator.md && grep -c "Downside compliance audited" .claude/agents/orchestrator.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - Orchestrator has a "4a2. Downside Compliance Audit" step between epistemic review and multiple consistency audit
    - Audit checks 4 things: floor calc present, heroic optimism resolved, TBV cross-check done, trough path complete
    - Non-compliant candidates are rejected with specific reason strings
    - TBV flag is a warning (confidence flag) not a rejection
    - Report methodology notes updated with downside compliance line
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full wiring:

1. Analyst references calc-score.sh floor: `grep "calc-score.sh floor" .claude/agents/analyst.md`
2. Analyst references strategy-rules.md: `grep "strategy-rules.md" .claude/agents/analyst.md`
3. Analyst references valuation-guidelines.md: `grep "valuation-guidelines.md" .claude/agents/analyst.md`
4. Orchestrator has compliance audit: `grep "Downside Compliance Audit" .claude/agents/orchestrator.md`
5. Orchestrator rejects non-compliant: `grep "downside non-compliant" .claude/agents/orchestrator.md`
6. No leftover "Reasonable Worst Case" in analyst: `! grep "Reasonable Worst Case" .claude/agents/analyst.md`
7. Both agents still valid markdown (no broken syntax)
</verification>

<success_criteria>
- Analyst Step 5 worst case fully replaced with 5-step structured process (Steps A-E)
- Analyst MUST run calc-score.sh floor before writing narrative (Step B is explicit)
- Analyst output format includes trough path table, floor command, TBV check
- Orchestrator has downside compliance audit with 4 checks and rejection reasons
- All existing agent functionality preserved (no regressions in other steps)
- Requirements ANLST-01 through ANLST-05 and ORCH-01 through ORCH-02 addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-integration/03-01-SUMMARY.md`
</output>
